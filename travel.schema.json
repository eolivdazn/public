{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/travel.schema.json",
  "title": "Travel Dashboard Frontmatter",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "title",
    "slug",
    "schema",
    "tripType",
    "startDate",
    "endDate",
    "year",
    "dateCounting",
    "places"
  ],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Canonical trip identifier: lowercase letters, digits and hyphens. Used as the output HTML filename and as tripSlug in the expenses API."
    },
    "favicon": {
      "type": "string",
      "description": "Emoji or icon representing the trip (e.g., 🥘 for Valencia, 🏯 for Thailand)"
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "One-sentence summary used as the page's meta description and Open Graph/Twitter Card caption when the trip page is shared."
    },
    "seoTitle": {
      "type": "string",
      "minLength": 1,
      "description": "Longer, keyword-rich title used for <title>/og:title/twitter:title (search and social surfaces). Falls back to '{title} Trip Guide — {startDate} to {endDate}' if omitted. The short 'title' field stays the display name used elsewhere in the app (dashboard trip picker, trip index list)."
    },
    "schema": {
      "type": "string",
      "const": "travel-dashboard/v1"
    },
    "tripType": {
      "type": "string",
      "const": "vacation"
    },
    "startDate": {
      "type": "string",
      "format": "date"
    },
    "endDate": {
      "type": "string",
      "format": "date"
    },
    "year": {
      "type": "integer",
      "description": "The trip's primary year. Must fall within startDate's and endDate's year."
    },
    "dateCounting": {
      "type": "string",
      "const": "inclusive"
    },
    "expenses": {
      "type": "object",
      "additionalProperties": true,
      "required": ["baseCurrency", "categories"],
      "properties": {
        "baseCurrency": {
          "type": "string",
          "minLength": 1
        },
        "partySize": {
          "type": "integer",
          "minimum": 1,
          "default": 2
        },
        "categories": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "flights": {
              "type": "number",
              "minimum": 0
            },
            "hotel": {
              "type": "number",
              "minimum": 0
            },
            "food": {
              "type": "number",
              "minimum": 0
            },
            "entertainment": {
              "type": "number",
              "minimum": 0
            },
            "transport": {
              "type": "number",
              "minimum": 0
            }
          },
          "required": ["flights", "hotel", "food", "entertainment"]
        }
      }
    },
    "places": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["city", "country", "startDate", "endDate"],
        "properties": {
          "city": {
            "type": "string",
            "minLength": 1
          },
          "region": {
            "type": "string"
          },
          "country": {
            "type": "string",
            "minLength": 1
          },
          "startDate": {
            "type": "string",
            "format": "date"
          },
          "endDate": {
            "type": "string",
            "format": "date"
          }
        }
      }
    }
  }
}
